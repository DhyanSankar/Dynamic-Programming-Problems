// Sample code to perform I/O:
#include <stdio.h>
#include<bits/stdc++.h>
using namespace std;
int main(){
	long long s, l, N;
	cin >> s >> ' ' >> l;
	cin >> N;
	vector<long long> arr(n), vis(100010, -1);
	for(int i = 0; i < N; i++){
		cin >> arr[i];
	}
	queue<int> que;
	que.push(s);
	visited[s] = 0;
	while(qu.empty() = false){
		//code
		for(long long i:arr){
			//code
		}
	}
}